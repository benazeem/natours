header.header
    nav.nav.nav--tours
        a.nav__el(href='/') All tours
        form.nav__search
            button.nav__search-btn 
                svg 
                    use(xlink:href='/img/icons.svg#icon-search')
            input.nav_search-input(type='text' placeholder='Search tours')
    div.header__logo
        img(src='/img/logo-white.png' alt='Natours logo')
    nav.nav.nav--user
        if user
            a.nav__el(href='#') My bookings
            a.nav__el(href='#')
                if user.photo
                 img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
                else 
                 img.nav__user-img(src=`/img/users/default.jpg` alt=`Photo of ${user.name}`)
                span= user.name
        else
            a.nav__el(href='/login') Log in
            a.nav__el.nav__el-cta Sign Up